% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hecateSearch.R
\name{hecateSearch}
\alias{hecateSearch}
\title{Search Hecate concepts and return results as a data frame}
\usage{
hecateSearch(
  query,
  vocabularyId = NULL,
  standardConcept = NULL,
  domainId = NULL,
  conceptClassId = NULL,
  limit = 20,
  client = hecateClient()
)
}
\arguments{
\item{query}{Character(1); search query (required).}

\item{vocabularyId}{Character(1) or NULL; optional vocabulary filter (comma-separated).}

\item{standardConcept}{Character(1) or NULL; e.g. \code{"S"} (Standard), \code{"C"} (Classification).}

\item{domainId}{Character(1) or NULL; optional domain filter (comma-separated).}

\item{conceptClassId}{Character(1) or NULL; optional concept class filter.}

\item{limit}{Integer(1); max results (default 20, max 150).}

\item{client}{Hecate client (default \code{hecateClient()}).}
}
\value{
Data frame of search results, or NULL if an error occurred (API error or bad response shape).
}
\description{
Search Hecate concepts and return results as a data frame
}
\examples{
\dontrun{
# Simple search
df <- hecateSearch("diabetes")

# Search with filters
df <- hecateSearch("hypertension", domainId = "Condition", limit = 10)
}
}
